## Latimore Hub (Vite)
## Copy to .env.local for local dev (do NOT commit secrets).

# Optional: passcode gate. If unset, no prompt.
VITE_HUB_PASSCODE=change-me

# Optional: Gemini key (client-side, local only)
VITE_GEMINI_API_KEY=
